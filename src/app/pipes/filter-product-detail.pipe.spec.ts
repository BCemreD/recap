import { FilterProductDetailPipe } from './filter-product-detail.pipe';

describe('FilterProductDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
